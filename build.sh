#!/bin/bash
# =============================================================================
# Bash 안전 모드 설정
# -e (errexit): 0이 아닌 exit 코드 발생 시 스크립트 즉시 종료
# -u (nounset): 선언되지 않은 변수 참조 시 에러로 처리
# -o pipefail: 파이프(|)로 연결된 명령어 중 하나라도 실패하면 전체를 실패로 처리
# =============================================================================
set -euo pipefail

echo "🔨 Building Next.js app..."
npm run build

echo "✅ Build completed!"
